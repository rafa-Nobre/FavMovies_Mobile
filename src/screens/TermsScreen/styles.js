import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    alignContent: 'center',
  },
  title: {
    marginTop: 10,
    fontSize: 20,
  },
  paragraph: {
    marginBottom: 15,
    fontSize: 12,
  },
});

export default styles;
